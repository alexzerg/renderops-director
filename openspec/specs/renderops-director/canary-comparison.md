# Canary Comparison

Version: 0.6.3

A successful canary is expected to resemble the reference, so show it beside the failed output at the same timecode and frame range. Side-by-side synchronized playback makes the removed artifact visible while preserving the distinction between a five-frame test and the final recovered shot.
