# Recovery Preview Navigation

Version: 0.7.1

Treat canary as a transitional state, not a permanent peer of final recovery. Return focus to generated media after each phase, provide a clear route back to the next approval, and replace transitional media once the final result exists. Run scroll after media metadata loads to avoid layout-shift offsets.
