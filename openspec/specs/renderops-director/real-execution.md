# Real Execution in a Reproducible Demo

Version: 0.7.0

A synthetic incident can still demonstrate real remediation. After human approval, execute bounded media processing, return the generated artifact, capture exit code, elapsed time, target frame count, byte size, and hash, then export these facts to observability and query them back before declaring success. Keep the synthetic failure boundary explicit.
