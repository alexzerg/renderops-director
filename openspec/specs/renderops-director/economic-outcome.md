# Economic Outcome Pattern

Version: 0.4.0

A domain agent should not stop at technical diagnosis. Parse authoritative cost metrics deterministically, calculate the bounded recovery cost and avoided cost in application code, and expose the result as typed API fields. Use the model to explain and contextualize the decision, not to perform or optionally omit the core arithmetic.
