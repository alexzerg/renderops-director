# Closed-Loop Recovery Pattern

Version: 0.6.0

A useful operations agent must verify its recommendation after approval. Emit a distinct canary telemetry phase, prove the expected metric/log/trace state, and only then unlock broader recovery. Keep deterministic checks authoritative over model prose. The model explains the evidence; application code controls approval state and pass/fail transitions.
