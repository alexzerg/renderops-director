# Video Failure Timeline

Version: 0.6.2

For partial frame failures, show when the artifact is active rather than implying the entire clip is corrupt. Silent media should use a custom transport without audio controls. Synchronize progress, timecode, failure windows, and an explicit active-failure indicator with playback time.
