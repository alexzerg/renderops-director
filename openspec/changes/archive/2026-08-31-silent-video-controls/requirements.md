# Silent Video Controls Requirements

Version: 0.6.2

Do not display native audio controls for silent preview media. Provide custom play/pause and scrub controls, dynamic timecode, visible failure windows, and an active failure indicator only while the failed render is inside a corrupted frame interval.
