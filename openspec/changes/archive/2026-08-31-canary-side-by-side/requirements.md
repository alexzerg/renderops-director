# Canary Side-by-Side Requirements

Version: 0.6.3

A successful canary should match the clean source, so its value must be demonstrated against the same failed frames rather than as a separate nearly identical clip. Show synchronized failed v47 and canary v46-safe outputs side by side for frames 1042–1046. Keep the full recovered shot separate.
