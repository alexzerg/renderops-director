# Verification Contract

Version: 0.1.0

The MVP is complete only when all commands in `test-plan.md` exit successfully, no critical issue remains, the GitHub repository is public, and the Cloud Run service returns HTTP 200. A missing Grafana service-account token blocks only the live Grafana deployment gate and must remain explicitly disclosed.
