# Structured Response Reliability

Version: 0.2.3

Preserve actual ADK MCP function responses and invoke a schema-constrained Gemini normalization pass only when the primary tool-using agent does not produce a valid structured payload. Never use canned evidence. Permit read-only Prometheus and Loki label discovery so the agent does not guess query labels.
