# Real FFmpeg Execution Requirements

Version: 0.7.0

Keep the initial GPU failure scenario synthetic, but make every approved canary and recovery operation execute real media processing inside the existing Cloud Run service. Return the newly generated WebM to the browser, export actual execution telemetry to Grafana, and require MCP verification of execution and render outcomes.
