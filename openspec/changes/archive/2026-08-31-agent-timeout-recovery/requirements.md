# Agent Timeout Recovery

Version: 0.3.1

The public investigation must never complete with `No final response received` or empty evidence/recovery sections. Bound the primary ADK loop, collect the same live Grafana signals through a parallel official read-only MCP session, and normalize those function responses with schema-constrained Gemini output.
